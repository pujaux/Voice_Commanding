services:
  - type: web
    name: voice-shop-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 4000
      - key: GROQ_API_KEY
        sync: false
