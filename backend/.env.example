PORT=4000

# Optional. Only needed for the Clarification Agent (LLM fallback for
# commands the rule-based parser can't classify). The app works fully
# without this — it just skips the LLM escalation step.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=
